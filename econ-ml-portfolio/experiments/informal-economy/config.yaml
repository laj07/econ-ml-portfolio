name: informal-economy
experiment_type: informal_economy

data:
  nightlights_dir: data/nightlights

exp_params:
  in_channels: 1        # single-band VIIRS luminosity
  pretrained: true
  dropout: 0.3
  lr: 1.0e-4
  batch_size: 32
  max_epochs: 30
  tile_size: 256
  seed: 42

checkpoint: checkpoints/informal_economy_best.ckpt

figures:
  scatter: output/informal_economy_scatter.png
